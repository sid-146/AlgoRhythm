from utils import logger, helpers
from datetime import datetime

"""
    IDEA
        Create a class of matric operation an it do all the operation on matrixs
"""


if __name__ == '__main__':
    path = helpers.get_path()
    logger = logger.get_logger(path=path, event="Matrix_Ops")

    logger.info("----"*30)
    logger.info(f'Process started at : {datetime.now()}')
    
    logger.info('Driver function called')
    driver = "This will be a function which will drive all the program"
